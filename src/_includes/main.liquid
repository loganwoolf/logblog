---
site: loganwoolf.dev!
---
<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>
      {% if title %}
        {{ title }} -
      {% endif %}
      {{ site }}
    </title>
  </head>

  <body class="min-h-screen flex flex-col text-lg">
    <header class="bg-gray-800 text-white mb-8 py-4">
      <nav class="flex flex-col items-center gap-4">
        <h1 class="text-4xl">
          <a href="/">
            logan woolf
            <span class="text-2xl">dot dev</span>
          </a>
        </h1>
        <ul class="flex items-center gap-6">
          {% for item in menuItems %}
            <li>
              <a class="text-lg block px-4 py-2 transition-colors duration-300 ease-linear hover:bg-gray-700" href="{{ item.slug }}">{{ item.title }}</a>
            </li>
          {% endfor %}
        </ul>
      </nav>
    </header>

    <main class="max-w-4xl mx-auto px-4 sm:px-0">
      {{ content }}
    </main>

    <footer class="bg-gray-900 text-white mt-auto p-2 text-center">
      <p>
        <strong>© Logan Woolf 2023</strong>
      </p>
    </footer>

  </body>

</html>
